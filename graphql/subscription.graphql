type Subscription {
    # messageAdded(chatroom_id: ID! @rules(apply: ["required"])): Chat!
    #     @middleware(checks: ["auth:api"])

    testingSubscription: String! @middleware(checks: ["auth:api"])
}
